
import React from 'react';
import { FilterSearch, FilterValues } from './FilterSearch';

// Re-export FilterSearch as FilterSearchTime for backward compatibility
export { FilterSearch, FilterValues };

// Also export a component with the same name as FilterSearchTime for backward compatibility
export const FilterSearchTime = FilterSearch;
